#include "Commands.h"

using namespace std;

/**
 * Constructors and destructors
 */
Command::Command(const char* cmd_line) {
    //argc = _parseCommandLine(cmd_line, argv);
}

Command::~Command() {

}

